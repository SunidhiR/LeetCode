class Solution:
    def findWordsContaining(self, words: List[str], x: str) -> List[int]:
        c=[]
        for i,j in enumerate(words):
            if x in j:
                c.append(i)
        return c
        
